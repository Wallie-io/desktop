// import { ALLOWED_FILE_TYPES } from '../config/s3';

//validationfile type in paremeter
export function validateFileType() {
//   return ALLOWED_FILE_TYPES.includes();
}

export default validateFileType;
